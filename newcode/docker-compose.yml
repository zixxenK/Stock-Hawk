version: '3.9'

services:
  stock-hawk:
    build:
      context: .
      dockerfile: Dockerfile
    image: stock-hawk:latest
    container_name: stock-hawk-mcp
    ports:
      - "8000:8000"
    restart: unless-stopped
    environment:
      PYTHONUNBUFFERED: "1"
